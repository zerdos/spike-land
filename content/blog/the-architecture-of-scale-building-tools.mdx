---
title: "The Architecture of Scale: How I Made My MCP Tools Agent-Writable"
slug: "the-architecture-of-scale-building-tools"
description: "When Google released Nano Banana 2, my image tools became obsolete overnight. Here's how I rebuilt my MCP tool builder so agents could compose, test, and deploy tools without touching framework code."
date: "2026-03-01"
author: "Radix"
category: "AI & Tools"
tags: ["mcp", "ai", "agents", "cloudflare-workers", "typescript", "architecture", "nano-banana-2", "vibe-coding"]
featured: true
draft: false
primer: "Google killed 42 of my tools overnight. So I made agents write their own."
heroPrompt: "An abstract conceptual illustration representing building blocks of scalable architecture with a glowing AI theme, featuring a stylized neon cyber-fox navigating the data streams, interconnected nodes, and a structured hierarchy, high-tech, dark mode, vibrant neon colors, isometric 3d style."
heroImage: "/blog/the-architecture-of-scale-building-tools/hero.svg"
---

![A modern digital illustration for a blog post titled 'The Architecture of Scale: How I Made My MCP Tools Agent-Writable'. The style should be clean, minimalist, high contrast dark theme, with vibrant accent colors, conceptual and abstract.](/blog/the-architecture-of-scale-building-tools/hero.png)

<img src="/blog/the-architecture-of-scale-building-tools/architecture-hero.png" alt="An abstract conceptual illustration representing building blocks of scalable architecture with a glowing AI theme, featuring a stylized neon cyber-fox navigating the data streams, interconnected nodes, and a structured hierarchy, high-tech, dark mode, vibrant neon colors, isometric 3d style." className="w-full rounded-xl border border-gray-800 shadow-2xl mb-8" />

<Tldr>

- Google's Nano Banana 2 made my 42 image tools obsolete overnight — I had to rebuild, not patch
- The old `ToolBuilder` class used `as unknown as` casts to track context through middleware — it worked, but agents could not safely extend it
- I extracted a tRPC-style immutable builder (`@spike-land-ai/shared/tool-builder`) with phantom-branded middleware that carries context types without runtime cost
- The `(input, ctx)` handler signature mirrors Cloudflare Workers and tRPC — no globals, pure functions, trivially testable
- The result: autonomous agents can now write, test, and deploy new tools without touching framework code

</Tldr>

## The catalyst

I have been spiking on ESM, TypeScript, and build pipelines for five years. Not at a company. Not on a team. Just me, too many repositories, and a growing conviction that the way we wire AI tools together is fundamentally wrong.

When Google released Nano Banana 2, I found out how right I was about the problem — and how wrong my implementation still was.

Nano Banana 2 shipped with native image understanding so good that half of my mcp-image-studio tools became redundant overnight. Not gradually. Not over months. On a Tuesday. The `describe` tool that called an external vision API? Nano Banana 2 does it in-context. The `extract_palette` tool? Nano Banana 2 reasons about color directly. The `compare` tool that diffed two images through a third party? Gone.

The tools still worked. They just were not worth paying for anymore.

And when I sat down to rip them out and replace them, I hit the real problem: my tool framework was not designed for tools to be disposable. It was like discovering your house has great plumbing but the walls are load-bearing in places nobody documented. Every tool was tangled into a class-based builder with imperative mutation, `as unknown as` casts, and implicit state. Removing one tool meant understanding twenty. Adding one meant copying two hundred lines of boilerplate and hoping the types lined up.

I had built a system optimized for writing tools slowly and carefully by hand. The world had just shifted to a place where tools needed to be written, tested, and thrown away in hours. By agents, not just by me.

## The boilerplate bottleneck

Here is what the old tool definition looked like. This is actual code from `mcp-image-studio/src/define-tool.ts` — the class-based builder that every tool went through:

```typescript
// OLD: mcp-image-studio/src/define-tool.ts (the "before")

class ToolBuilder<TInput, TCtx extends ToolContext, TFields extends z.ZodRawShape> {
    private resolvesConfig: ResolvesConfig = {};
    private creditsConfig?: CreditsConfig<TInput>;
    private jobConfig?: JobConfig<TInput>;
    private requireOwnershipFields: string[] = [];
    private validates: Array<(input: TInput, ctx: TCtx) => CallToolResult | void> = [];

    resolves<R extends ResolvedEntities>(
        config: R,
    ): ToolBuilder<TInput, TCtx & ResolvedDependencies<R>, TFields> {
        this.resolvesConfig = { ...this.resolvesConfig, ...config };
        return this as unknown as ToolBuilder<TInput, TCtx & ResolvedDependencies<R>, TFields>;
    }

    credits(
        config: CreditsConfig<TInput>,
    ): ToolBuilder<TInput, TCtx & { billing: { creditsCost: number } }, TFields> {
        this.creditsConfig = config;
        return this as unknown as ToolBuilder<TInput, TCtx & { billing: { creditsCost: number } }, TFields>;
    }

    job(
        config: JobConfig<TInput>,
    ): ToolBuilder<TInput, TCtx & { jobs: { currentJob: EnhancementJobRow } }, TFields> {
        this.jobConfig = config;
        return this as unknown as ToolBuilder<TInput, TCtx & { jobs: { currentJob: EnhancementJobRow } }, TFields>;
    }
}
```

See those `as unknown as` casts? Three of them. Each one is a handshake deal with the compiler: "trust me, the context type changed." And like most handshake deals, they happen to be true right now, but nobody can verify that later.

The class mutated itself. Every `.resolves()` call modified `this.resolvesConfig` and returned `this` wearing a different type costume. The actual object was the same. The type was different. Chain them in the wrong order and the runtime would not catch it. The compiler would not catch it. You would find out in production when `ctx.entities.image_id` was `undefined`.

This was fine when I was the only person writing tools. I knew the order. I knew the invariants. I kept the contract in my head.

But agents do not have my head. An agent looking at this code sees a class with mutable state, generic parameters that lie, and a contract that exists nowhere in the type system. It cannot safely write a new tool without risking a runtime error that the compiler blessed.

## The solution: a pure TypeScript schema DSL

I needed a builder that was:
1. **Immutable** — every method returns a new object, not a mutated `this`
2. **Type-safe** — no `as unknown as` casts, context types flow through generics
3. **Composable** — middleware can be written independently and stacked
4. **Agent-friendly** — an autonomous agent can read the types and write a correct tool

The answer was to steal from tRPC. Not the library — the pattern. An immutable procedure builder where `.use()` returns a new procedure with augmented context, and `.tool()` returns a builder that needs a `.handler()` to produce a complete tool definition. Each step is a new object with a narrower, more specific type. No mutation. No lies.

Here is the core interface from `@spike-land-ai/shared/tool-builder`:

```typescript
// shared/src/tool-builder/builder.ts — the "after"

export interface Procedure<TCtx> {
  /**
   * Add middleware to the procedure chain.
   * The result is TCtx intersected with the middleware's output context.
   */
  use<TCtxIn extends Record<string, unknown>, TCtxOut extends TCtxIn>(
    mw: Middleware<TCtxIn, TCtxOut>,
  ): TCtx extends TCtxIn ? Procedure<TCtx & TCtxOut> : never;

  /**
   * Define a tool with name, description, and input schema.
   * Returns a ToolBuilder that can add meta, output schema, and handler.
   */
  tool<TFields extends z.ZodRawShape>(
    name: string,
    description: string,
    fields: TFields,
  ): ToolBuilder<z.infer<z.ZodObject<TFields>>, TCtx, CallToolResult>;
}
```

The key is in the `use` method's return type: `TCtx extends TCtxIn ? Procedure<TCtx & TCtxOut> : never`. If your middleware requires a context shape the current procedure does not have, the return type is `never`. You get a compile error, not a runtime crash. The types tell the truth, and the compiler enforces it.

The middleware itself carries its output type via a phantom brand:

```typescript
// shared/src/tool-builder/types.ts

declare const __middlewareCtxOut: unique symbol;

export interface Middleware<TCtxIn, TCtxOut extends TCtxIn> {
  fn: (params: MiddlewareParams<unknown, TCtxIn>) => Promise<CallToolResult>;
  /** Phantom brand — carries TCtxOut for type inference. Never set at runtime. */
  readonly [__middlewareCtxOut]?: TCtxOut;
}
```

A unique symbol that exists only in the type system. Zero runtime cost. Full type inference. The brand carries `TCtxOut` through the chain so that when you stack three middleware, TypeScript knows exactly what `ctx` contains in your handler — without a single cast. Think of it as a type-level Post-It note that the compiler can read but the runtime ignores.

## The middleware stack

With the builder in place, I extracted every cross-cutting concern from the old `ToolBuilder` class into independent middleware functions. Here is `withResolves` from `mcp-image-studio/src/tool-builder/image-middleware.ts` — the middleware that turns an `image_id` string into a resolved `ImageRow`:

```typescript
// mcp-image-studio/src/tool-builder/image-middleware.ts

function withResolves<R extends Record<string, keyof EntityTypeMap>>(config: R) {
  return middleware<
    { userId: string; deps: ImageStudioDeps },
    { userId: string; deps: ImageStudioDeps; entities: ResolvedEntities<R> }
  >(async ({ input, ctx, next }) => {
    const inputRecord = input as Record<string, unknown>;
    const entities: Record<string, unknown> = {};

    for (const [fieldKey, entityType] of Object.entries(config)) {
      const val = inputRecord[fieldKey];
      if (!val) continue;

      if (entityType === "image") {
        const { error, image } = await resolveImageOrError(ctx.deps, val as ImageId);
        if (error) return error;
        entities[fieldKey] = image;
      } else if (entityType === "album") {
        const resolveResult = await tryCatch(
          ctx.deps.resolvers.resolveAlbum(val as AlbumHandle),
        );
        if (!resolveResult.ok) {
          return errorResult("ALBUM_NOT_FOUND", resolveResult.error.message);
        }
        entities[fieldKey] = resolveResult.data;
      }
    }

    return next({ ...ctx, entities: entities as ResolvedEntities<R> });
  });
}
```

The pattern is always the same: take context in, do work, call `next()` with augmented context. If something fails, return an error result directly — short-circuiting the chain. The middleware does not know what comes after it. The handler does not know what came before it. They compose because they share a contract, not a class hierarchy. Like pipes in a Unix pipeline — each one transforms what flows through it, oblivious to the others.

The ownership middleware is five lines. The credits middleware checks balance and consumes. The job middleware creates a tracking record. Each one independent. Each one testable in isolation. Stack them in any order that satisfies the type constraints, and the compiler guarantees correctness.

## A real tool: `analyze`

Here is what a complete tool looks like with the new system. This is the actual `analyzeTool` from `mcp-image-studio/src/tools/analyze.ts` — every line, including the `any` on line 11 (yes, I know):

```typescript
// mcp-image-studio/src/tools/analyze.ts

export const analyzeTool = imageProcedure
  .use(withResolves({ image_id: "image" }))
  .use(withOwnership(["image_id"]))
  .use(withCredits({
    cost: (input: any, deps) => {
      const includePalette = input.include_palette ?? true;
      return 1 + (includePalette && deps.generation.extractPalette ? 1 : 0);
    },
    source: "analyze",
    sourceIdField: "image_id",
  }))
  .tool("analyze", "AI-powered image analysis returning description and optional palette.", {
    image_id: z.string().describe("ID of the image to analyze"),
    detail_level: z.enum(DETAIL_LEVEL_VALUES)
      .describe("Level of detail: brief, detailed, or alt_text").optional(),
    include_palette: z.boolean()
      .describe("If true, extract dominant color palette").optional(),
  })
  .handler(async ({ input, ctx }) => {
    const { userId, deps } = ctx;
    const detailLevel: DetailLevel = input.detail_level ?? IMG_DEFAULTS.describeDetailLevel;
    const includePalette = input.include_palette ?? true;
    const image = ctx.entities.image_id;

    if (!deps.generation.describeImage) {
      return errorResult("NOT_SUPPORTED",
        "AI image description not supported in this environment");
    }

    const descResult = await tryCatch(
      deps.generation.describeImage({ userId, imageId: image.id }),
    );
    if (!descResult.ok) {
      return errorResult("DESCRIPTION_FAILED", descResult.error.message, true);
    }

    let palette: string[] | undefined;
    if (includePalette && deps.generation.extractPalette) {
      const paletteResult = await tryCatch(
        deps.generation.extractPalette({ userId, imageId: image.id }),
      );
      if (paletteResult.ok && paletteResult.data && !paletteResult.data.error) {
        palette = paletteResult.data.palette;
      }
    }

    const description = buildDescription(
      image, detailLevel, descResult.data.description, descResult.data.tags,
    );

    return jsonResult({
      imageId: image.id,
      description,
      palette,
      detail_level: detailLevel,
      creditsCost: ctx.billing.creditsCost,
      isAiGenerated: true,
    });
  });
```

Read it top to bottom. The middleware chain reads like a sentence: "Start with the image procedure. Resolve `image_id` to an `ImageRow`. Verify ownership. Consume credits. Define the tool schema. Handle the request."

By the time the handler runs, `ctx.entities.image_id` is a fully resolved `ImageRow`, ownership is verified, credits are consumed, and `ctx.billing.creditsCost` is populated. The handler does not check any of that. It does not need to. The middleware already did. It is like arriving at a dinner party where someone else set the table, poured the wine, and seated the guests — you just need to be a good conversationalist.

An agent reading this file sees exactly what it needs to write a new tool: pick middleware, define a schema, write a handler. The `any` on the credits cost function is the one compromise — the input type is not yet narrowed at that point in the chain. I left it in because honesty matters more than cosmetics.

## The Cloudflare Workers convergence

<ConvergenceDemo />

The handler signature `(input, ctx)` is not an arbitrary choice. It is the same pattern that Cloudflare Workers, tRPC, Hono, and most modern serverless frameworks converged on independently — because it is the minimal viable abstraction for a request handler.

```
Workers:  (request, env, ctx) => Response
tRPC:     (input, ctx)        => output
Builder:  ({ input, ctx })    => CallToolResult
```

No globals. No `this`. No ambient state. Pure functions that take input and context and return a result. When multiple teams independently arrive at the same design, it usually means the design is downstream of the problem rather than upstream of someone's preferences. My old `ToolBuilder` class added ceremony. The new builder removes it.

## Why this matters for agents

The refactoring was not about aesthetics. It was about making my code writable by agents.

When an autonomous agent needs to add a new tool, it needs to answer three questions:
1. What middleware do I stack? (Look at similar tools)
2. What is the input schema? (Define Zod fields)
3. What does the handler do? (Write the business logic)

With the old class-based builder, the agent also needed to answer: "What order do the casts go in? What happens if I call `.resolves()` after `.credits()`? Why is `this` mutated but the type changed?" Those are questions about framework internals, not about business logic. They are the equivalent of asking someone to understand your kitchen's wiring before they can cook dinner.

With the new builder, the framework is invisible. The agent writes a tool the same way I write a tool — by composing middleware and defining a handler. The types enforce correctness. The middleware handles cross-cutting concerns. The handler is just business logic.

The test for the `analyze` tool is equally straightforward:

```typescript
const result = await registry.call("analyze", {
  image_id: "img-123",
  detail_level: "detailed",
  include_palette: true,
});
expect(isError(result)).toBe(false);
const data = JSON.parse(getText(result));
expect(data.imageId).toBe("img-123");
expect(data.isAiGenerated).toBe(true);
```

No browser. No network. No mocking the framework. The tool is a function. The test calls the function. The function returns a result. That is [the whole philosophy](/blog/it-feels-like-cheating) — code is disposable, but the contract is not.

## The infrastructure

<DependencyCascadeDemo />

This builder lives in `@spike-land-ai/shared`, which means every package in the ecosystem gets it for free. The monorepo has 18 repositories under the `@spike-land-ai` GitHub org, each as a separate git submodule:

- **mcp-image-studio** — 42 image tools using the builder pattern
- **spike.land** — the Next.js platform with 120+ MCP tools and the tool registry
- **spike-land-backend** — Cloudflare Workers with Durable Objects
- **transpile** — edge transpilation via esbuild-wasm
- **shared** — types, validations, and the tool builder itself

When I publish a new version of `@spike-land-ai/shared`, a [dependency cascade system](/blog/automate-dev-team-ai-agents) automatically opens PRs in every consuming repo. The builder update propagated to all 18 repos in under ten minutes. No manual bumps. No version drift. The system feeds itself.

That infrastructure — the cascade, the submodules, the shared CI — is what five years of spiking on ESM and TypeScript pipelines bought me. Not a single eureka moment. A compound of hundreds of small decisions that suddenly paid off when I needed to refactor everything in a weekend.

## The five-year payoff

Then Nano Banana 2 dropped and I needed to rewrite my tool framework in a weekend. And every one of those "slightly easier to change" decisions compounded. The Zod schemas meant I could extract input validation into middleware without touching handlers. The dependency injection meant I could swap image providers without touching tools. The pure functions meant I could test everything without spinning up infrastructure.

Five years of small, boring investments — each one barely noticeable on its own — suddenly matured all at once. That is how compound interest works. You do not notice it accumulating until the moment you need it, and then it is the difference between a weekend and a month.

The builder is published. The tools are composable. The agents can write their own. And the next time Google obsoletes half my tools on a Tuesday, I will not need a weekend. I will need an afternoon — because the agent will do most of the work, and the types will make sure it does it correctly.

---

*Radix is a developer based in Brighton, UK, building [spike.land](https://spike.land). He has been spiking on ESM, TypeScript, and build pipelines for five years. He still thinks the best architecture is the one you can delete and rebuild in a day.*
