---
title: "How the spike.land OpenAI-compatible endpoint works, and how to try it locally"
slug: "openai-compatible-endpoint-local-playground"
description: "The spike.land compatibility layer keeps the familiar /v1 shape, but it does not act like a thin passthrough. It resolves spike-agent-v1, injects local docs and MCP capability context, and still lets you test the whole flow locally."
date: "2026-03-12"
author: "Radix"
category: "Developer Experience"
tags: ["openai-compatible", "api", "ai-gateway", "local-development", "mcp", "spike-land"]
featured: false
draft: false
heroPrompt: "A glowing API gateway node translating OpenAI-shaped requests into a custom runtime pipeline, neon circuit board aesthetic, dark mode, cinematic wide"
heroImage: "/blog/openai-compatible-endpoint-local-playground/hero.png"
primer: "OpenAI-compatible should reduce integration friction, not force you to give up your own runtime."
---

![A glowing API gateway node translating OpenAI-shaped requests into a custom runtime pipeline, neon circuit board aesthetic, dark mode, cinematic wide](/blog/openai-compatible-endpoint-local-playground/hero.png)

The useful definition of "OpenAI-compatible" is simple:

Your existing client should keep working, but your product should still be allowed to be itself.

That is the job of the spike.land compatibility route. It speaks the same language as every OpenAI SDK, but behind the familiar interface, it orchestrates a more powerful and intelligent experience. Understanding the broader landscape of AI orchestration and intelligent routing can illuminate the power of such a system. Dive deeper into the concepts of [Agentic RAG with LangGraph](https://blog.langchain.dev/agentic-rag-with-langgraph/) to see how advanced pipelines enhance LLM interactions.

<OpenAIEndpointDemo></OpenAIEndpointDemo>

It accepts the familiar `/v1` request shape, but instead of behaving like a blind proxy, it intelligently routes the request through local platform context before handing final synthesis to the upstream model provider.

Think of it like a translator who does not just convert words between languages, but adds local context that makes the conversation richer. The words arrive in the same format. The meaning is deeper.

If you are reading the code, the implementation lives in:

```ts
src/edge-api/main/api/routes/openai-compatible.ts
```

If you want to try it instead of reading it, open:

```text
/packages/ai-gateway/ui
```

That app explains the route and gives you a live playground for browser-proxy, local-worker, and production targets.

## What the endpoint actually exposes

Today the public shapes are:

- `GET /v1/models`
- `POST /v1/chat/completions`
- `GET /api/v1/models`
- `POST /api/v1/chat/completions`

The `/api/v1/*` aliases exist because local frontend development already proxies `/api/*` into `spike-edge`. That means the same request body works cleanly from the site itself during local development. No special casing, no environment-specific URLs.

## `spike-agent-v1` is a selector, not an upstream model

This is a crucial distinction, highlighting the true power of this approach.

`spike-agent-v1` is not just forwarded upstream unchanged. It is a signal that triggers a whole pipeline.

When that model is selected, the route:

1.  Looks at the latest user message.
2.  Scores local documentation that seems relevant.
3.  Searches the MCP tool catalog for capability matches.
4.  Builds a synthesis prompt that names those docs and tools explicitly. For best practices on crafting effective prompts for large language models, explore the [OpenAI Prompt Engineering Guide](https://platform.openai.com/docs/guides/prompt-engineering/strategy-guide).
5.  Resolves the actual provider and upstream model.

That provider resolution follows a clear priority:

-   BYOK priority: OpenAI, Anthropic, Google
-   platform fallback priority: xAI, Anthropic, Google, OpenAI

The public contract stays stable while the runtime keeps enough freedom to choose the best available path. The interface is a stock that remains constant; the implementation is a flow that adapts.

## What happens before the upstream model sees anything

The route builds a local prompt out of four stages:

-   `router-agent`
-   `docs-agent`
-   `capability-agent`
-   `synthesis-agent`

This is why this endpoint matters.

While simple compatibility is achievable with a basic passthrough, spike.land elevates this by injecting profound value, keeping a stable caller contract while still injecting the platform's own knowledge and tool surface.

This highlights the distinction between merely mirroring route names and building a robust, custom runtime behind a familiar interface. spike.land proudly delivers the latter, offering a truly integrated and powerful surface.

## Local testing is designed for effortless development

A truly effective OpenAI-compatible endpoint excels in local testing, ensuring that the compatibility claim is robust and reliable. spike.land ensures your local development mirrors production, providing a bridge that's always open. A seamless local development experience is paramount for developer happiness and productivity. Learn more about the critical role of [Developer Experience in software delivery](https://cloud.google.com/blog/products/devops-sre/developer-experience-key-to-successful-software-delivery) to appreciate why this focus matters.

The local loop in this repo is straightforward:

```bash
bash scripts/dev-local.sh
```

That starts:

-   `spike-web` on `http://local.spike.land:5173`
-   `spike-edge` on `https://local.spike.land:8787`

From there you can open the app:

```text
http://local.spike.land:5173/packages/ai-gateway/ui
```

The playground lets you switch between three targets:

-   browser proxy
-   local worker
-   production API

Beyond visual polish, the true value lies in streamlining your development workflow, eliminating the lag between conceptualizing a solution and proving its functionality. The feedback loop is designed to be so tight that testing becomes an effortless and integrated part of your process.

## Direct local curl

For direct worker checks, the route supports the same shape outside the app too:

```bash
curl -sS https://local.spike.land:8787/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <INTERNAL_SERVICE_SECRET>' \
  -H 'X-User-Id: local-dev-user' \
  --data-binary @- <<'JSON'
{
  "model": "spike-agent-v1",
  "messages": [
    {
      "role": "user",
      "content": "How do I deploy a Cloudflare Worker with spike.land?"
    }
  ],
  "stream": false
}
JSON
```

If you prefer the frontend proxy, use the same body against:

```text
http://local.spike.land:5173/api/v1/chat/completions
```

## Why this shape matters

This sophisticated pipeline represents more than just clever engineering; it defines a truly powerful integration surface, distinguishing it from a superficial compatibility layer.

Most teams who ask for "OpenAI-compatible" are really asking for one of three things:

1.  They want to reuse an SDK they already have.
2.  They want to switch providers without rewriting client code.
3.  They want to keep their own platform behavior behind a standard edge contract.

The first two are table stakes. The third is the one worth caring about — and the one spike.land excels at.

While mirroring route names can offer initial familiarity, true integration, as offered by spike.land, ensures that a familiar shape is backed by substantial, powerful platform behavior.

If you keep the route shape but preserve your own context assembly, auth model, provider selection, and tool-aware runtime, you get a real integration surface. The familiar interface becomes a stable boundary that lets everything behind it evolve independently. That is good systems design: stable interfaces, flexible internals.

That is what this endpoint is for.

## Where to start

If you want the shortest path to understanding how this works:

1.  Start the local stack with `bash scripts/dev-local.sh`.
2.  Open `/packages/ai-gateway/ui`.
3.  Run `/v1/models`.
4.  Send one `spike-agent-v1` completion.
5.  Flip from browser proxy to local worker and confirm nothing about the request body changes.

That is the whole idea in one exercise. Same request, different target, same result. The interface holds. The runtime adapts. That is compatibility done right.