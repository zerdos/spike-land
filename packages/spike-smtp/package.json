{
  "name": "@spike-land-ai/spike-smtp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "build": "echo 'CF Worker — no build step required'",
    "deploy": "wrangler deploy --minify",
    "w:deploy:prod": "wrangler deploy --minify",
    "typecheck": "tsc --noEmit",
    "lint": "eslint --quiet .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260418.1",
    "@vitest/coverage-v8": "^4.1.4",
    "eslint": "^10.2.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4",
    "wrangler": "^4.83.0"
  },
  "source": "./index.ts"
}
