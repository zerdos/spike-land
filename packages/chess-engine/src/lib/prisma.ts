// Prisma stub — chess-engine does not have a generated Prisma client.
// `any` is required here because 20+ call sites across game-manager,
// player-manager, and challenge-manager expect concrete model types
// (ChessPlayer, GameRecord, etc.) from Prisma query methods.
// Fix: run `prisma generate` to create a real client.
// dprint-ignore
export default {} as any; // eslint-disable-line @typescript-eslint/no-explicit-any
