/**
 * Lie Detector MCP Tools (CF Workers)
 *
 * Fact-checking and claim verification tools using AI. Not yet fully migrated from spike.land.
 */

import type { ToolRegistry } from "../mcp/registry";
import type { DrizzleDB } from "../db/index";

export function registerLieDetectorTools(
  _registry: ToolRegistry,
  _userId: string,
  _db: DrizzleDB,
): void {
  // Not yet migrated from spike.land
}
