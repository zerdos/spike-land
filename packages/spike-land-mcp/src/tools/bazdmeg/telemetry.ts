/**
 * BAZDMEG Telemetry MCP Tools (CF Workers)
 *
 * Usage telemetry and performance monitoring tools. Not yet fully migrated from spike.land.
 */

import type { ToolRegistry } from "../../mcp/registry";
import type { DrizzleDB } from "../../db/index";

export function registerBazdmegTelemetryTools(
  _registry: ToolRegistry,
  _userId: string,
  _db: DrizzleDB,
): void {
  // Not yet migrated from spike.land
}
