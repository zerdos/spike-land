/**
 * BAZDMEG Skill Sync MCP Tools (CF Workers)
 *
 * Skill synchronization and cross-workspace sharing. Not yet fully migrated from spike.land.
 */

import type { ToolRegistry } from "../../mcp/registry";
import type { DrizzleDB } from "../../db/index";

export function registerBazdmegSkillSyncTools(
  _registry: ToolRegistry,
  _userId: string,
  _db: DrizzleDB,
): void {
  // Not yet migrated from spike.land
}
