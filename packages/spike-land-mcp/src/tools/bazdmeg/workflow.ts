/**
 * BAZDMEG Workflow MCP Tools (CF Workers)
 *
 * Workflow automation and task pipeline tools. Not yet fully migrated from spike.land.
 */

import type { ToolRegistry } from "../../mcp/registry";
import type { DrizzleDB } from "../../db/index";

export function registerBazdmegWorkflowTools(
  _registry: ToolRegistry,
  _userId: string,
  _db: DrizzleDB,
): void {
  // Not yet migrated from spike.land
}
