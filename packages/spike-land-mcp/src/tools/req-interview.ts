/**
 * Requirements Interview MCP Tools (CF Workers)
 *
 * AI-guided requirements gathering and specification tools. Not yet fully migrated from spike.land.
 */

import type { ToolRegistry } from "../mcp/registry";
import type { DrizzleDB } from "../db/index";

export function registerReqInterviewTools(
  _registry: ToolRegistry,
  _userId: string,
  _db: DrizzleDB,
): void {
  // Not yet migrated from spike.land
}
