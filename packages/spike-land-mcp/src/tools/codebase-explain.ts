/**
 * Codebase Explain MCP Tools (CF Workers)
 *
 * AI-powered codebase explanation and documentation generation. Not yet fully migrated from spike.land.
 */

import type { ToolRegistry } from "../mcp/registry";
import type { DrizzleDB } from "../db/index";

export function registerCodebaseExplainTools(
  _registry: ToolRegistry,
  _userId: string,
  _db: DrizzleDB,
): void {
  // Not yet migrated from spike.land
}
