{
  "name": "@spike-land-ai/math-engine",
  "version": "0.1.0",
  "type": "module",
  "description": "AI-powered math engine — multi-agent proofs, Erdos conjectures, audit gap resolution",
  "license": "MIT",
  "main": "mcp/index.ts",
  "bin": {
    "math-engine": "mcp/index.js"
  },
  "scripts": {
    "start": "tsx mcp/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project math-engine",
    "test:coverage": "vitest run --project math-engine --coverage",
    "lint": "eslint --quiet ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "@spike-land-ai/mcp-server-base": "workspace:*",
    "zod": "4.4.2"
  },
  "devDependencies": {
    "tsx": "4.21.0",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
