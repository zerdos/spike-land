# CLAUDE.md

## Overview

Vite + React 19 + TanStack Router SPA for the spike.land frontend. This package
is one of the first-party clients of the open MCP app store: it renders the
store UI, app-facing pages, and discovery/install surfaces on top of the same
runtime other origins can call directly. Talks to `spike-edge` (via `/api`
proxy). Published as `@spike-land-ai/spike-app`.

## Commands

```bash
npm run dev           # Vite dev server (proxies /api → localhost:8787)
npm run build         # tsc --noEmit && vite build
npm run preview       # Serve production build locally
npm run typecheck     # tsc --noEmit
npm run lint          # ESLint
npm test              # Vitest run
npm run test:coverage # Vitest with v8 coverage
```

## Architecture

```
├── main.tsx              # App entry — AuthProvider, RouterProvider
├── routes/
│   ├── __root.tsx        # Root layout (sidebar nav)
│   ├── index.tsx         # Dashboard
│   ├── analytics.tsx     # Analytics
│   ├── callback.tsx      # OIDC callback handler
│   ├── login.tsx         # Login page
│   ├── settings.tsx      # Settings
│   ├── store.tsx         # App store
│   ├── apps/             # Apps routes ($appId.tsx, index.tsx, new.tsx)
│   ├── messages/         # Messages routes ($userId.tsx, index.tsx)
│   └── tools/            # Tools routes ($category.tsx, index.tsx)
├── components/
│   ├── AppCard.tsx
│   ├── AuthGuard.tsx
│   ├── ChatThread.tsx
│   ├── LivePreview.tsx
│   ├── LoginButton.tsx
│   ├── StatusBadge.tsx
│   └── VersionHistory.tsx
├── hooks/
│   ├── useAnalytics.ts   # Page-view tracking (event queue, batched flush)
│   └── useAuth.ts        # Wraps Better Auth client
├── lib/
│   └── auth.ts           # Auth config (Better Auth client)
├── routeTree.gen.ts      # Auto-generated by TanStack Router — do not edit
├── test-setup.ts         # Vitest + @testing-library/jest-dom setup
└── app.css               # Tailwind v4 base styles
```

### Routing

TanStack Router with file-based route tree. `routeTree.gen.ts` is auto-generated
— run `npm run dev` to regenerate after adding routes. Route files live under
`routes/`; nested directories create nested routes.

### State Management

No global state library. State lives in:

- **OIDC context** — Better Auth for auth (see `lib/auth.ts`)
- **Local component state** — `useState` for UI-only state (sidebar open/close,
  etc.)

### Auth

Better Auth via `lib/auth.ts`. Supports GitHub and Google providers. The
`/callback` route handles the redirect.

### Styling

Tailwind CSS v4 via `@tailwindcss/vite` plugin. No config file — uses CSS-first
configuration in `app.css`. Path alias `@/` maps to the package root.

### Dev Proxy

`vite.config.ts` proxies `/api/*` to `http://localhost:8787` (spike-edge). Start
spike-edge with `npm run dev` in `src/spike-edge` before running the
frontend for full-stack local dev.

## App Store Notes

- `ui/routes/store.tsx` renders the first-party store catalog.
- The store route consumes `/api/store/tools`, which aggregates public MCP
  metadata into a UI-friendly catalog.
- App pages should preserve the “MCP app = composable tools + metadata” model
  instead of drifting into screenshot-only marketing pages.

## Environment Variables

| Variable              | Default                             | Purpose      |
| --------------------- | ----------------------------------- | ------------ |
| `VITE_OIDC_AUTHORITY` | (configured per environment)        | OIDC authority |
| `VITE_OIDC_CLIENT_ID` | `spike-platform-client-id`          | OIDC client ID |

## Code Quality Rules

- TypeScript strict mode
- Never use `any` — use `unknown` or proper types
- Never add `eslint-disable` or `@ts-ignore` comments
- `routeTree.gen.ts` is auto-generated — never edit manually
- Tests use `@testing-library/react` + `vitest` with `jsdom` environment
- All new components should have a corresponding `.test.tsx`
