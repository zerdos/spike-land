// Lazy-loaded external shim: re-exports the Prism highlighter component as a
// separate chunk so syntax highlighting is not included in the main bundle.
export { Prism } from "react-syntax-highlighter";
