// Vite build entry point: re-exports @emotion/styled (named + default) as a
// stable chunk for consistent import paths across the app.
export * from "@emotion/styled";
export { default } from "@emotion/styled";
