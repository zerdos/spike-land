// External shim: re-exports renderApp from the internal render-app library
// as a named entry point for dynamic imports and the service worker.
export { renderApp } from "../../../ui/@/lib/render-app";
