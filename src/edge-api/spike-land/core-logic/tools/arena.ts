/**
 * Arena MCP Tools (CF Workers)
 *
 * Leaderboard and competition arena tools. Not yet fully migrated from spike.land.
 */

import type { ToolRegistry } from "../../lazy-imports/registry";
import type { DrizzleDB } from "../../db/db/db-index.ts";

export function registerArenaTools(_registry: ToolRegistry, _userId: string, _db: DrizzleDB): void {
  // Not yet migrated from spike.land
}
