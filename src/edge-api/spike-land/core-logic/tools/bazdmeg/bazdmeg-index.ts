/**
 * BAZDMEG Core MCP Tools (CF Workers)
 *
 * Core BAZDMEG workspace automation tools. Not yet fully migrated from spike.land.
 */

import type { ToolRegistry } from "../../../lazy-imports/registry";
import type { DrizzleDB } from "../../../db/db/db-index.ts";

export function registerBazdmegTools(
  _registry: ToolRegistry,
  _userId: string,
  _db: DrizzleDB,
): void {
  // Not yet migrated from spike.land
}
