/**
 * Decisions MCP Tools (CF Workers)
 *
 * Decision tracking and architecture decision records (ADR). Not yet fully migrated from spike.land.
 */

import type { ToolRegistry } from "../../lazy-imports/registry";
import type { DrizzleDB } from "../../db/db/db-index.ts";

export function registerDecisionsTools(
  _registry: ToolRegistry,
  _userId: string,
  _db: DrizzleDB,
): void {
  // Not yet migrated from spike.land
}
